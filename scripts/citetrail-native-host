#!/bin/sh
script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
project_root=$(CDPATH= cd -- "$script_dir/.." && pwd)
PYTHONPATH="$project_root${PYTHONPATH:+:$PYTHONPATH}" exec python3 -m citetrail native-host 2>>/tmp/citetrail-native-host.log
